problem: max area of island

we are given an m x n binary matrix grid. an island is a group of ones (like yesterday's problem). 

the area of an island is the number of cells with a value 1 in the island.

return the maximum area of an island in grid. if no island, return 0.

right so i've got to basically redo the algorithm from yesterday except instead of incrementing islands, do some manipulation to calculate the max area and then compare it with the current value in variable. if greater than, replace that.

i'm going to implement it using dfs because that was the simplest.

alright i am not getting the correct answer but that's okay! i'm getting 3 instead of 6, i think it's because i'm restarting the area variable everytime i call dfs because i defined it within the function, and when there's an island that has more than just four squares around it dfs would be called again which means that the area will be restarted again. 

oops i forgot to mark the cell that we visited a 0 so we don't visit it again.

i passed in area as a parameter but somehow i'm still one short of the correct answer for test case 1.

my theory is that this is happening because when it reaches the base case it doesn't get time to increment. SO MAYBE i'll increment at the start.

dangit it didn't change anything. so maybe the update is not working properly?

wait i just debugged and printed area and it does calculate to 6 at one point but for some reason the variable isn't being updated to 6.

hm i passed the two regular test cases but failed a main one.

okay the issue with this is that i'm passing area through as a parameter.

i see we were meant to return the actual value of the current island using the dfs function.

maaayuun i was so close it's ok i understand 

dfs solution:
we define our dfs function, and do the base cases as usual. we set the current cell to 0 so we don't revisit it, and then return 1 + all 4 directions for dfs recursively called on our current cell.
```python
class Solution(object):
    def maxAreaOfIsland(self, grid):
        """
        :type grid: List[List[int]]
        :rtype: int
        """
        rows, columns = len(grid), len(grid[0])

        def dfs(r, c):
            if r < 0 or c < 0 or r >= rows or c >= columns or grid[r][c] == 0:
                return 0

            grid[r][c] = 0
            return (1 + dfs(r + 1, c) + dfs(r - 1, c) + dfs(r, c + 1) + dfs(r, c - 1))
        
        area = 0
        for r in range(rows):
            for c in range(columns):
                if grid[r][c] == 1:
                    area = max(area, dfs(r, c))                  

        return area
```