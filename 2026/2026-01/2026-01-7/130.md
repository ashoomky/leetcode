problem: surrounded regions

given a matrix where every connecting "o" is a region.
"x" surrounds the region IF you can connect to other x cells AND none of the region cells are on the edge of the board.

to capture a surrounded region, replace all 'o's with 'x's in place within the original board.

right so to capture we just essentially want to get rid of the region as a whole and replace the os with xs only if they fulfill the criteria above.

my initial thought is to use dfs, as we would just need to search around the current o region area, not do a level by level sweep. i think.

in the base case to stop this function, we need to check if the r and c are out of bounds, and if the O is on an edge, because then we want to return.

alrighty i've got a maximum recursion depth exceeded error. there's an error with my base case i'm assuming.

oh wait i forgot to do the base checks in the directions for loop.

hm it's still exceeding the recursion depth. 

ohhh okay so my recursion error is because i am constantly avoiding the edge cells, which means that it keeps running dfs on the inner cells again and again and again.

which means i need a new method.

if i can't mark off the edge cells like that, maybe i can 

okay hint 2 says to reverse the approach and mark the regions that are reachable from the *border* O's. 

with a boolean variable?

i'm confused though how would reaching the regions from the border's O work, because that just means that entire region is already not catchable because it touches the edge?

ohhhh okay i see so we run dfs on every direction from the edge O and mark it a #, which means the entire section will not be capturable.

then run the dfs algorithm on each edge where the cell's have an O, and then finally traverse the graph with a nested for loop and replace all O's with X, and all # with O.

```python
class Solution(object):
    def solve(self, board):
        """
        :type board: List[List[str]]
        :rtype: None Do not return anything, modify board in-place instead.
        """
        rows, columns = len(board), len(board[0])
        directions = [[0, 1], [0, -1], [1, 0], [-1, 0]]

        def dfs(r, c):
            if (r < 0 or c < 0 or r == rows or c == columns or board[r][c] != "O"):
                return
            board[r][c] = "#"
            for dr, dc in directions:
                new_row, new_col = r + dr, c + dc
                dfs(new_row, new_col)

        for r in range(rows):
            if board[r][0] == "O":
                dfs(r, 0)
            if board[r][columns - 1] == "O":
                dfs(r, columns - 1)
        
        for c in range(columns):
            if board[0][c] == "O":
                dfs(0, c)
            if board[rows - 1][c] == "O":
                dfs(rows - 1, c)

        for r in range(rows):
            for c in range(columns):
                if board[r][c] == "O":
                    board[r][c] = "X"
                elif board[r][c] == "#":
                    board[r][c] = "O"

```

