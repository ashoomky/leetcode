problem: subtree of another tree

this seems to be similar to yesterdays problem, so i'm going to try implement that and see.

first we will return False if there is no node on our current pointer, which is our base case.
wait hold on how do we continuously check for each subtree against only one huh
cos we don't want to stop when we get to the last node of subroot, we just want to check the subroot again but against a different part of the root or something

okay i basically rewrote yesterdays solution but it didn't work on the case i knew it wouldn't work on- where there is a subtree but it returns false because it stops checking once the subtree has been finished traversing. 

okay so we want to compare the subtree to each node and return true if the subtrees match. from the previous question, we know how to save the left and right subtrees and compare them.

so i added 3 lines of code lol
basically checking if the helper function returns true, then running the code recursively on the left and right subtrees of the original tree and returning those results. 
```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def isSubtree(self, root, subRoot):
        """
        :type root: Optional[TreeNode]
        :type subRoot: Optional[TreeNode]
        :rtype: bool
        """
        if not root:
            return False
        
        def sameTree(root1, root2):
            if not root1 and not root2:
                return True
            
            if root1 and root2 and root1.val == root2.val:
                left = sameTree(root1.left, root2.left)
                right = sameTree(root1.right, root2.right)
                return left and right
            else:
                return False
        
        if sameTree(root, subRoot):
            return True
        
        return self.isSubtree(root.left, subRoot) or self.isSubtree(root.right, subRoot)

```