problem: invert binary tree

initial thought is to invert the leaves first, then keep inverting each node above that until we've reached the root. we can do this using recursion.

okay but how do you essentially know when you've reached the root of the tree, and how do we output this in list form?

hint 1 says there's an algorithm that can help traverse the tree.
hint 2 says to use DFS (depth first search) algorithm. at each node, we swap its left and right children by swapping their pointers. this inverts the current node, but every node in the tree also needs to be inverted. to achieve this, we recursively visit the left and right children and perform the same operation. if the current node is null, we return.

ohhh so when you return the tree it just automatically outputs in a list format. 

so basically we want to recursively call on the left and right children and swap their pointers. okay.

for every root node, we want to switch the childs. 

```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def invertTree(self, root):
        """
        :type root: Optional[TreeNode]
        :rtype: Optional[TreeNode]
        """
        
        if not root:
            return None 
        
        temp_right = root.right
        root.right = root.left
        root.left = temp_right
        self.invertTree(root.left)
        self.invertTree(root.right)
        return root
```