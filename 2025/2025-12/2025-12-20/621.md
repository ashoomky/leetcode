problem: task scheduler

we are given an array of CPU `tasks`, each labeled with a letter from A to Z, and also given a number n. 

each CPU interval can be idle or allow the completion of ONE task. 
tasks can be completed in any order, but there has to be a gap of at least n intervals between two tasks with the same label.

return the **minimum** number of CPU intervals required to complete all tasks.

wow i really do not get this question.

okay wait no i get it but how the heck am i supposed to code this up.

okay so basically you have to find the minimum number of CPU intervals required to complete all tasks, so we have to find a possible sequence where it uses minimal tasks possible, and adheres to the requirement that two tasks with the same level must have at least n number of intervals (other tasks or idle) in between before it can be run again.

okay i get it.

since i know this is a priority queue / heap question, i should be using a heap. the question is, min or max heap?

okay i would say to use a min heap because we want to get the minimum sequence possible, so we can just return the first element of the heap. 

hint 1 says there are at most 26 different tasks, A to Z. ooh it is more efficient to count the frequency of each task and store it in a hash map or an array of size 26. 
and tasks at the start of the alphabet should be run first.

nope we actually process the most frequent task first oops. when we have the most frequent task, using our hashmap, we need to ensure that it's not processed again after n seconds. they say to use a max heap to retrieve the max element and another data strcture to cooldown the processed tasks.

oh im introduced to the queue data structure now. we use it to hold off reinserting the processed task into the heap, maybe by appending idle instead? hmm but how would you implement that

sigh okay so we start by calculating the frequency of each task and initialise a variable called time to track the total processing time.
the task frequencies are inserted into a max heap. 
use a queue to store tasks along with the time they become available after the cooldown - this confuses me.
at each step, if the max-heap is empty, we update time to match the next available task in the queue, covering idle time. 
else, we process the most frequent task from the heap, decrement its frequency, and if its still valid then add back to the queue with its next available time. 
if the task at the front of the queue becomes available, pop it and reinsert into the heap. 

i pushed the frequencies as a tuple (-freq, letter) as sorting is based on the first element in a heap.

now to implement a queue structure... might need a refresher lowkey forgot.
wait is that not just a heap? they are the same thing? maybe?

oh they did q.dequeue(). oopshaha i did too many ueueu
*^they did deque()*

okay so we want to process the more frequent tasks first because that guarantees at least the minimum, if we did them all last then we'd have to have a lot of idle times.

oh the solution didn't store the actual alphabet in the max heap either - i guess we don't need to keep track of it so that makes sense. 

okay so it makes sense now ! we count the count in a max heap. first we increment the time by one, and we pop the max element in max heap as that would be the current most frequent task. decrement the count of the value we just popped from the max heap by adding one, and then check if the count is greater than 0, and if it is then append to the queue as a `[count, time + n]` pair as the second index is the time the time variable needs to be before being able to push this count to the heap again. 
then, to check for that condition if the time is equal and ready to be pushed to max heap - we check the second index of the first element in the queue against the time, and if it is true then push it to the heap. 
then return the time !!
```python
class Solution(object):
    def leastInterval(self, tasks, n):
        """
        :type tasks: List[str]
        :type n: int
        :rtype: int
        """
        # could use count = Counter(tasks) - pythons built in hashmap
        frequency_dict = {}
        for i in tasks:
            if i not in frequency_dict:
                frequency_dict[i] = 1
            else:
                frequency_dict[i] += 1

        max_heap = [-count for count in frequency_dict.values()]
        heapq.heapify(max_heap)

        time = 0
        queue = deque() # pairs of [count, idletime]
        while max_heap or queue:
            time += 1
            if max_heap:
                # add 1 because we're using negative values, actually decrementing it
                count = 1 + heapq.heappop(max_heap)
                # now checking if there is still tasks left 
                if count:
                    queue.append([count, time + n])

            if queue and queue[0][1] == time:
                heapq.heappush(max_heap, queue.popleft()[0])
        return time
   
```




