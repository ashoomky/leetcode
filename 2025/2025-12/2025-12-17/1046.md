problem: last stone weight

we choose the two heaviest two stones and smash them together. let's call them x and y. where x <= y.

1. if x == y, both stones are destroyed.
2. if x != y, the stone of weight x is destroyed, and the weight of y is now equal to y - x.

at the end of the game, there is at most one stone left. return the weight of the last remaining stones. if no stones left, return 0.

i would think we use a max heap to access the first two elements of the array because they will be the maximum.

okay so i can implement a max heap by negating the values, then when i take them out i negate them again.

okay so i did all the logic however its saying that my list index is out of range? so something must be wrong, let me debug by printing stones within the while loop.

oops i was assigning the position in the array by indexing instead of pushing using the heap properties.

okay now there's something wrong with my return value, it's negative even though i'm extracting it while negating it.

OKAY i realised i was doing the calculation without negating them again oopsees

pleasework
YAY IT WORKED wow but my runtime sucks 

beats 34.63%
```python
class Solution(object):
    def lastStoneWeight(self, stones):
        """
        :type stones: List[int]
        :rtype: int
        """
  
        for i in range(len(stones)):
            stones[i] = -stones[i]
        heapq.heapify(stones)
       
        while stones:
      
            if len(stones) == 1:
                return -stones[0]

            x = -stones[1]
            y = -stones[0]
            if x == y:
                heapq.heappop(stones)
                heapq.heappop(stones)
            else:
                old_y = heapq.heappop(stones)
                # destroy x
                x = heapq.heappop(stones)
                heapq.heappush(stones, -((-old_y) - (-x))) 
        return 0

```

mmmneetcode has a very clean solution
they popped the first two elements either way without an if statement which makes sense. 

then to return if there was nothing in the stones array they appended 0 and returned the absolute value of stones. i seeeee.


