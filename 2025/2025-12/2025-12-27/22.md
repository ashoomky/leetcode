problem: generate parantheses

so we have to return a list of all "combinations of well-formed parantheses"

oh dear

so they have to be valid

hint 2 says to use backtracking with pruning? what is pruning D:
what makes a string invalid?
when there is no closing bracket - oh when there's an uneven number of brackets - but that still doesn't fully make sure.

hint 3: when the count of closing brackets exceeds the count of opening brackets, the string becomes valid. 
ohhhh
maintain two variables: open and close, to track the number of opening and closing brackets

we avoid exploring paths where close > open. once the string length reaches 2n, add to result. 

okay i looked at the solution cos im so confused

we only build only valid strings, instead of generating all strings.

key rules:
- we can only add `(` if you still have openings left (open < n)
- you can add ')' only if it won't break validity (close < open)
- a string is complete and valid only when open == close == n


```python
class Solution(object):
    def generateParenthesis(self, n):
        """
        :type n: int
        :rtype: List[str]
        """
        stack = []
        result = []

        def backtrack(open_bracket, closed_bracket):
            if open_bracket == closed_bracket == n:
                result.append("".join(stack))
                return
            
            if open_bracket < n:
                stack.append("(")
                backtrack(open_bracket + 1, closed_bracket)
                stack.pop()
            if closed_bracket < open_bracket:
                stack.append(")")
                backtrack(open_bracket, closed_bracket + 1)
                stack.pop()
        
        backtrack(0, 0)
        return result

            
```