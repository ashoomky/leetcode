problem: design a simplified version of twitter where users can post tweets, unfollow/follow another user and is able to see the 10 most recent tweets in the user's news feed.

wow this is kind of a confusing structure to wrap my head around?

okay so the function we use the max heap in is getNewsFeed as we need to retrieve the 10 most recent tweet IDs in the user's news feed. each item in the news feed must be posted by users who the user followed, or by the user themself. okay that makes sense. must be ordered from most recent to least recent.
in this function it seems we are returning a list of all the tweet ids in preceding order (latest to earliest).

wow that's a tall order

okay so we use a max heap and we store values as (count, tweetid) as count is our way of keeping track of what tweet we're on. HOWEVER we need to consider the condition of the tweets only being able to be there if the user themselves posted it, or if someone they're following posted it.

thus we have an if condition when popping from the heap, and if it doesn't satisfy then don't append to the list we're returning.

basically we heapify the value of the userId in the tweet_map


wow what a crazy question. 
in the heap we keep track of four values: count, tweetId for returning value / appending to result list, followeeId for accessing the next recent tweets, and the index which tells us the next tweet to access. 
we go through each follower of the current user id by accessing the follow map / set, and also add themselves to the set because they are technically following themselves. 
we decrement the count because we are adding to a max heap, thus will order the tweets accordingly.
we use a hashset because of the remove function, this makes it easier access and so we don't have to go through an entire list to find the one we want to remove. better time complexity.

```python
class Twitter(object):

    def __init__(self):
        self.follow_map = defaultdict(set) # stores user ids and followees
        self.tweet_map = defaultdict(list) # stores user ids and their tweet as a list of (count, tweet ids) pairs
        self.count = 0 # tracks number of tweets

    def postTweet(self, userId, tweetId):
        """
        :type userId: int
        :type tweetId: int
        :rtype: None
        """
        self.tweet_map[userId].append([self.count, tweetId])
        self.count -= 1

    def getNewsFeed(self, userId):
        """
        :type userId: int
        :rtype: List[int]
        """
        result = [] # ordered starting from most recent
        min_heap = []

        #since a user can technically follow themselves, add them to the followset 
        self.follow_map[userId].add(userId)
        for followeeId in self.follow_map[userId]:
            if followeeId in self.tweet_map:
                index = len(self.tweet_map[followeeId]) - 1 # index of the most recent tweet of who user follows 
                count, tweetId = self.tweet_map[followeeId][index] # count and tweet id of who user follows most recent tweet
                min_heap.append([count, tweetId, followeeId, index - 1]) # keep track of followee id because we need it to get the next most recent tweet, and index - 1 to access the previous tweet (next most recent)
        heapq.heapify(min_heap)

        while min_heap and len(result) < 10:
            count, tweetId, followeeId, index = heapq.heappop(min_heap)
            result.append(tweetId)
            if index >= 0:
                count, tweetId = self.tweet_map[followeeId][index] # tells us the next tweet to add to our heap
                heapq.heappush(min_heap, [count, tweetId, followeeId, index - 1])
        return result


    def follow(self, followerId, followeeId):
        """
        :type followerId: int
        :type followeeId: int
        :rtype: None
        """
        self.follow_map[followerId].add(followeeId)

    def unfollow(self, followerId, followeeId):
        """
        :type followerId: int
        :type followeeId: int
        :rtype: None
        """
        if followeeId in self.follow_map[followerId]:
            self.follow_map[followerId].remove(followeeId)





# Your Twitter object will be instantiated and called as such:
# obj = Twitter()
# obj.postTweet(userId,tweetId)
# param_2 = obj.getNewsFeed(userId)
# obj.follow(followerId,followeeId)
# obj.unfollow(followerId,followeeId)
```