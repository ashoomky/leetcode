problem: search in rotated sorted array

with knowledge from the last problem, my initial thought is to first find the minimum of the array, then concatenate the first half and then second half to a new array variable, then run binary search on that array.

okay well i have successfully un rotated the array and performed binary search on the original array which works, but i realised i have to return the index of the element in the original array which i tried to do using nums.index(new_array[m]) but it didn't seem to work for some test cases.

i realised now the issue is in a list of 2, the array is not unrotating like it should be. this is an annoying bug because it works for normal array sizes.

i also added a condition above that if the number was not in the array then return -1, so the entire code wouldn't run if the number isn't even present.

so to fix the issue above, i changed the way i concatenated the two lists. and you may ask how? i actually don't know i'm still figuring that out i thought by typing that sentence it would lead me to a revelation but it has not.

oh crap i also just realised .index has linear time complexity of O(n) and not logn.

now i have two issues to fix:
1. concatenating the sorted array correctly
2. finding the index of the element in the rotated array

OH WAIT i might not even have to concatenate a sorted array ! i can just perform binary search on the sub sorted sections of the array once i've found the cut!

i got stuck on the finding the index within the given array part. 

oh the solution used helper functions... that makes sense...

OH they defined it within the function we're supposed to make.

solution: 
first find the pivot.
after finding the pivot, create a helper function of binary search to then use after to search the first half of the list first, and if it's not in the first half then search in the second half and return the index of that. to distinguish between the first and second half of the list we passed in different left and right pointers, so for the first half of the list we passed in 0, pivot - 1 as the left and right pointers respectively. for the second half of the list, we passed in pivot, len(nums) - 1.
```python
class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        # finding pivot point
        l, r = 0, len(nums) - 1
        while l < r:
            m = (l + r) // 2
            if nums[m] > nums[r]:
                l = m + 1
            else:
                r = m
        pivot = l

        def binary_search(left, right):
            while left <= right:
                m = (left + right) // 2
                if nums[m] == target:
                    return m
                elif nums[m] > target:
                    right = m - 1
                else:
                    left = m + 1
            return -1
        
        # running search on first half of the list first
        result = binary_search(0, pivot - 1)
        if result != -1:
            return result
        
        # running search on second half if not in first half
        return binary_search(pivot, len(nums)-1)
        
```