problem: binary tree right side view

literally just return the nodes you see from the right side.

initial thought is to traverse through the right side, and if there's nodes on the left side that only have one tree on each level then also add those.
so we would need to keep track of the number of nodes on each level, which we can use our knowledge from the previous question.

1. traverse the tree level by level
2. only obtain the second node on levels that have two nodes, and for levels with one node just append that

i've got the algorithm to traverse each level and append every node on each level, now i just need some manipulation of code to make it so that i only append the second element.

okay i figured out the issue i just needed to call the right subtree recursive call first as it would run that first and append the node to the result. 

dfs solution: for this solution, we only append the node to the result array if the depth is equal to the length of the current result array we have. this ensures that if length is longer, that means we've already appended for that level and therefore we do not need to anymore. 
```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def rightSideView(self, root):
        """
        :type root: Optional[TreeNode]
        :rtype: List[int]
        """
        
        result = []
        def dfs(node, depth):
            if not node:
                return None
            if depth == len(result):
                result.append(node.val)
            dfs(node.right, depth + 1)
            dfs(node.left, depth + 1)
            
        
        dfs(root, 0)
        return result
        
```

bfs solution: with this solution, the right_side variable keeps getting overwritten, leaving us with the right most node at the end of every iteration. this makes sense as we want that to happen. same with the previous solution, it freezes the queue length when iterating over the queue before appending the next level.
```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def rightSideView(self, root):
        """
        :type root: Optional[TreeNode]
        :rtype: List[int]
        """
        
        result = []

        queue = deque([root])
        while queue:
            right_side = None
            queue_length = len(queue)
            for i in range(queue_length):
                node = queue.popleft()
                if node:
                    right_side = node
                    queue.append(node.left)
                    queue.append(node.right)
            if right_side:
                result.append(right_side.val)
```