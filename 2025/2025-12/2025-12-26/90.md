problem: subsets 2

same as subsets 1 except this time the input array has duplicate numbers. 

is it not just like combination sum 2?

so the two decisions would be: 
1. keep dfs with the current index
2. keep skipping the current element if it's equal to the one next to it.
and of course we need to sort the input list first


im getting a maximum recursion depth and i'm not sure why... maybe my base case is wrong or something

oh it's cause i was constantly running dfs on the same index i forgot to put + 1

YAY wow this problem was a combination of subsets and combination 2 - i just needed to use my knowledge of how to not get duplicate subsets and it was a piece of cake

yay even though i got my hand held throughout this question i am proud of myself it means i am learning yippee
```python
class Solution(object):
    def subsetsWithDup(self, nums):
        """
        :type nums: List[int]
        :rtype: List[List[int]]
        """
        result = []
        nums.sort()
        def dfs(i, combination):
            if i == len(nums):
                result.append(combination[:])
                return
            
            combination.append(nums[i])
            dfs(i + 1, combination)
            combination.pop()

            while i + 1 < len(nums) and nums[i] == nums[i+1]:
                i += 1
            dfs(i + 1, combination) 
        
        dfs(0, [])
        return result
```