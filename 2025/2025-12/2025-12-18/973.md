problem: K closest points to origin

im thinking to use a min heap to easily access the most minimum element? and push the calculated distance onto the heap.
then reutrn the coordinate with that minimum distance

hm i would need to keep track of what the distance is for each coordinate so i can access it to return it - maybe a hashmap

okay a hashmap where the key is the euclidian distance, and the value is the coordinate (type is list of lists)
then a min heap where i push the distance onto and retrieve the minimum element, then access its respective value through the hashmap.

oh we need to return the k minimum points. so if k = 2 we return the 2 minimum points.

so that means we would get the first two elements of the array! amazing

okay so i implemented that solution however i failed one test case where the output came out reversed? like it got inputted as [0, 1] but then [1, 0] got outputted so i'm confused.

oh i see it's cause they have the same distance so the second point overrode the first one in the dictionary.

to get past this i'm going to just append if the key already exists.

okay but that doesn't work because now it appends the entire list itself.

hmmmmmmmmmm 

oh wow the hint says to use a max heap? ohhhh they say if the size of the heap exceeds k, they remove the farthest point (so the minimum is maintained). then convert heap to array and return it. surely i can do that. but max heaps makes more sense cos when you pop it just pops the max element.

okay this does make sense because then we're utilising the properties of a heap.

okay i'm still confused on how to keep track of the point that is in correlation to its distance, because a dictionary won't work as i've learnt from before.

maybe i can push it as a tuple, with the point as the second value?

YAHOO i did it
only beats 25% of runtime complexity but it's okay, i think it's cause of my second for loop to access the values haha.

```python
class Solution(object):
    def kClosest(self, points, k):
        """
        :type points: List[List[int]]
        :type k: int
        :rtype: List[List[int]]
        """

        max_heap = []
        heapq.heapify(max_heap)

        for i in range(len(points)):
            distance = math.sqrt((points[i][0]*points[i][0]) + (points[i][1] * points[i][1]))
            heapq.heappush(max_heap, [-distance, points[i]])
            if len(max_heap) > k:
                heapq.heappop(max_heap)
        
        result = []
        for j in range(len(max_heap)):
            result.append(max_heap[j][1])
        return result


```

oh the solution has a min heap solution as well, i see they decremented k while popping values from the min heap and when it equaled 0 they return.

both heaps append the distance, x and y point to the heap which is the same as what i did !!

yipppppeeeeee