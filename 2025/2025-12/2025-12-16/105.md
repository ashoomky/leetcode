problem: construct binary tree from preorder and inorder traversal

we are given two arrays - preorder and inorder where preorder is the preorder traversal of a binary tree (we see the root node first, left node then right node.) and inorder is the inorder traversal of the same tree (left node first, then root then right node).

my first thought is that we would need to use both arrays, since we have been given two arrays ?

i would say i use preorder traversal to create the tree because this way we can append in the correct order. 

also good to note it's a binary tree so so the left node must always be smaller than the root, the right node must be greater than.

the inorder traversal helps divides the array into two halves based on the root node. yes we know this (hint 1 says this) and it says can you think of how we can get the index of the root node in the in-order array?

yes we can use pre-order traversal.

uh okay wait hint 2 says from the pre-order traversal, we know that the first node is the root node. now can we construct the binary tree? uhhhh

hint 4 says we can use a hash map to get the index of any node in the in-order array in O(1) time.

how do we implement this what the flippityfloppp

oh hint 5 says we use dfs to construct the tree. global variable tracks the current index in the pre-order array. indices l and r represent the segment in the in-order array for the current subtree. 

i am so confusedconfusedconfused

they said for each node in the pre-order array, create a node, find its index in the inorder array using the hash map and recursively build the left and right subtrees by splitting the range [l, r] into two parts for the left and right subtrees. 

so basically...
we use the inorder list to access the left and right subtree nodes of the current node using left and right pointers. we create a hashmap where the key is the value of the inorder value, and its value is the index of the value in the inorder list. this is so we can access the left and right nodes of the current node we are on. we then assign the left and right subtrees to the respective indexed arrays, the left array from the root being the left subtree and the right the right. recursively call the function on the left and right to construct the tree.
```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def buildTree(self, preorder, inorder):
        """
        :type preorder: List[int]
        :type inorder: List[int]
        :rtype: Optional[TreeNode]
        """
        inorder_set = {}
        for i in range(len(inorder)):
            inorder_set[inorder[i]] = i
        
        # tracking index in preorder array
        self.preorder_index = 0

        def dfs(l, r):
            if l > r:
                return None
            
            root_value = preorder[self.preorder_index]
            self.preorder_index += 1
            root = TreeNode(root_value)
            # accessing the index of the root value in the inorder list
            mid = inorder_set[root_value]
            root.left = dfs(l, mid - 1)
            root.right = dfs(mid + 1, r)
            return root
        
        return dfs(0, len(inorder) - 1)



```