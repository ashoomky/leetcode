problem: lowest common ancestor of a binary search tree

okay so given a bst, find the lowest common ancestor node of two given nodes in the bst.

the lowest common ancestor is defined between two nodes p and q, and the LCA is the lowest possible ancestor shared between nodes p and q in the tree T. note that we also allow nodes to be a descendent of itself.

okeydokey

i'd say we have to somehow traverse the tree to find p and q, and on the way traversing keep track of the nodes and halt the search when we reach the desired node.
then compare the stored values (the saved nodes we traversed through to get to the nodes) to each other, and return the node they have in common.

another thought i can use to my advantage is that this tree is a binary search tree, so i can immediately know what part of the tree the target node is based on the current node we're on.

so i'd say we can traverse through this tree recursively?

using dfs?

yes okay so we recursively traverse through the tree, with conditions satisying properties of a bst. 

okay my main issue is figuring out how to keep track of the LCA for BOTH NODES when we're recursively traversing.

oh wait hint 3 has a good point - when it splits we then find the common lowest ancestor to be the node we're currently on.

okay i've figured out how to recursively call and return the right root, however now i need to find a condition that considers cases where the LCA is the target as well, as the definition allows this.

well i've spent the past 20 minutes debugging and i'm now at a place where i'm 99% sure my code works, but the 1% is that the condition still isn't working sighsighsigh

this is the condition that isn't working, for cases where the answer is the same node we're currently on.
```python
if not root or not p or not q:
            return None
 
        if p.val > root.val and q.val > root.val:
            self.lowestCommonAncestor(root.right, p, q)
        elif p.val < root.val and q.val < root.val:
            self.lowestCommonAncestor(root.left, p, q)
        else:
            return root
```

i realised (real eyes) haha that it returns null for that specific condition because i'm not returning what the condition finds, thus it returns None. 

yayyy and it works!! live laugh love leetcode

```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None

class Solution(object):
    def lowestCommonAncestor(self, root, p, q):
        """
        :type root: TreeNode
        :type p: TreeNode
        :type q: TreeNode
        :rtype: TreeNode
        """
        
        if not root or not p or not q:
            return None
 
        if p.val > root.val and q.val > root.val:
            return self.lowestCommonAncestor(root.right, p, q)
        elif p.val < root.val and q.val < root.val:
            return self.lowestCommonAncestor(root.left, p, q)
        else:
            return root
        
```