problem: count good nodes in a binary tree

given a binary tree, a node X in the tree is good IF the path from root to X has no nodes with a value greater than X.

so basically any node that is less than the current node is not good.

so we need to recursively call the tree to access the nodes and compare to lowest node in the current path.

we can traverse using dfs

so i passed the usual test cases, but i failed a test case while submitting.

i think i'm maybe misunderstanding the question?
ohh so for this case i'm somehow choosing a ndoe that is less than - which means i need to compare with the last seen node instead of the minimum? 

hmm now this gives me one less than the correct answer, and it's cause it's comparing a left and right subtree which both can be good nodes cos they're on different paths, but my algorithm doesn't know that and is instead comparing nodes on the same level.

maybe some bfs would be good here?

nope let's stick to dfs LOL

okay we should track the maximum along the current path. current path being the key point. 

okay i tried declaring the max_node as a local variable within my dfs function but i realised everytime it runs it will be set back to 0. so that would not work. 

okay so i got rid of trying to use the global variable and instead initialised a local variable within the dfs function, and recursively added to the count within there. i passed in a parameter called max_node instead of declaring it inside the function, and this way the max_node stays constant within the current path we're in. 

```python
# Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def goodNodes(self, root):
        """
        :type root: TreeNode
        :rtype: int
        """

       

        def dfs(node, max_node):
            if not node:
                return 0
            
            if node.val >= max_node:
                count = 1
            else:
                count = 0
        
            max_node = max(max_node, node.val)
            count += dfs(node.left, max_node)
            count += dfs(node.right, max_node)
            return count
        
        return dfs(root, root.val)

```