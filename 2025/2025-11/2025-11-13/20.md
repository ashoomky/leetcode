onto stacks now YAY ...
problem: return if there are valid parantheses, for example each opening bracket has its own closing bracket

initial thought is to push the opening brace onto stack, then if the current one we're on is the closing one pop it. if stack not empty by the end then return false, if empty then return true.

doing this i forgot about an edge case and ran into an issue where if the string just starts with a closing bracket, it's false. fixed this by adding an if statement to check the first element of the string and return false if its a closing bracket.
however i can't hardcode that because what if we encounter a closing bracket first?

hmmmm

we have to check if there's a closing bracket and it doesn't match the popped 

okay i just made else statement for each if statement if it didn't match to return false so the program would stop running immediately.

```python
    stack = []  

        for n in s:
            if n == "(" or n == "[" or n == "{":
                stack.append(n)
            elif n == ")" and stack:
                if stack[-1] == "(":
                    stack.pop()
                else:
                    return False
            elif n == "]" and stack:
                if stack[-1] == "[":
                    stack.pop()
                else:
                    return False
            elif n == "}" and stack:
                if stack[-1] == "{":
                    stack.pop()
                else:
                    return False
                   
            else:
                return False
        
        if stack:
            return False
        return True
```