problem: remove nth node from end of list
given an index n, remove that node from the linked list

initial thought is to keep a counter so we know if we reach the nth node, then save the next node after the current and point the previous node to the next next one.

okay i don't really get the test cases, it says to remove the nth node from the end of the list, but in this case we have [1,2,3,4,5] and n = 2. i would've thought the nth node is then 2, because 1, 2, but it's actually 4? and even if i start indexing from 0 it still doesn't reach 4. so i think i'm misinterpreting something here.

OH chatgpt said to count from the back of the list bruh. oh. that's what "from the end of the list" means aha

i see now why my test cases were failing 😀and how this is a medium😀

i will still have a counter to keep track of what node we're on.
thinking about how to do this in one pass but might have to try two first and just see if i can do it.
first i'll traverse the linked list to find the nth node, then store it in a variable. i'll create a dummy node for this 
then i'll go through the list normally, except this time  

WAIT maybe i can create a dummy node and just do everything in that for one pass? maybe?

i got a memory limit exceeded what
okay now its tle
i'm not skipping the node the right way

alrighty i'm stuck

the problem i'm having right now is being able to remove a node at the end of the list. i keep getting "None type can have next attribute" blahblah because there literally is nothing except for void after that node, but i need to remove it somehow, while also accounting for other test cases

nvm i just realised i can't compare the value of the node with the current cos what if there's multiple nodes with the same value

i just realised i ditched my entire dummy node idea
maybe it's time to go back to that

okay i reversed the linked list, now trying to figure out how to keep track of the nth node and know what it is.

on 5th thought, maybe i can find the nth node to remove by simply minusing the length of the linked list by n which would give us the node before the one we need to remove, then can do some withcraft (make that node's next pointer to the node after it) therefore deleting it.
who cares if i have redundant code i just wanna try solve this myself sigh

bruh how do you use a dummy node

holy crap i'm so close i'm just missing the first element of the linked list which is interesting uh

i have a solution except it doesn't work for removing the last element, as the while loop only runs when there is a current and a next, however in a list of 2 it'll just stop after the first iteration.

i finally got it but i had to have a lot of hints sigh

basically i was right about finding the length of the list and then minusing it from the target. then i got stuck testing my edge cases within my while loop because that way i ran into indexing errors within the linked list, for example i couldn't access the next value because well there was none.
instead, i made a while loop that only ran when remove variable was greater than 0, and that way it would stop on the current value i need to remove. 
then, i tested for an edge case before attaching the last part of the list, where if we are removing the first element of the list previous would be None, so i stored the next value and just returned from that while making the head of the list point to None. 
```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def removeNthFromEnd(self, head, n):
        """
        :type head: Optional[ListNode]
        :type n: int
        :rtype: Optional[ListNode]
        """

        # retrieving length of linked list
        previous, current = None, head
        length = 0
        while current:
            length += 1
            previous = current
            current = current.next
        
        if length == 1:
            return None 

        # removing nth node
        remove = length - n
        previous, current = None, head

        while remove > 0:
            previous = current
            current = current.next
            remove -= 1

        if previous == None:
            temp = current.next
            current.next = None
            return temp
        
        # attaches everything
        previous.next = current.next

        return head
```




