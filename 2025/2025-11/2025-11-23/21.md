problem: merge two sorted lists

linked list terminology: we have the head as the current node, head.next as the next.

initial thought is to go through the list and while current is not null, put the pointer to the next node in the other linked list and keep alternating until the current is none in one of the lists.
well i basically described the problem again lol.

the syntax is so weird help 

hint 2 says to create a dummy node to keep track of the head of resulting linked list while iterating through the lists.
hint 3 basically tells me how to implement it by iterating through both lists, move pointers by comapring the node values from both lists. link the next pointer of the iterator to the node with the smaller value. 

okay but what is our iterator, like a for loop? is it the dummy node? what is a dummy node? 

is it two seperate iterating through each lists?

OH we create a new linked list essentially, that's what the "dummy" node is for (might've peeked at answers for that cos i was stumped)

oh sigh looked at solution and it makes a lot of sense! simple but it's okay i'm learning. learnt a new technique to avoid the edge case of adding to an empty list we just create a dummy node, and return the next of that as it is essentially the head of the list we are returning (where we started adding things)
```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def mergeTwoLists(self, list1, list2):
        """
        :type list1: Optional[ListNode]
        :type list2: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        dummy = ListNode()
        tail = dummy

        while list1 and list2:
            if list1.val < list2.val:
                tail.next = list1
                list1 = list1.next
            else:
                tail.next = list2
                list2 = list2.next
            tail = tail.next
        
        
        if list1:
            tail.next = list1
        elif list2:
            tail.next = list2
        return dummy.next

        
 
        
```


