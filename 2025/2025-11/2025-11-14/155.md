problem: write code that implements a min stack

pretty self explanatory.

nevermind i forgot how to access variables in python classes 

i fixed it by instantiating the variable stack as self.stack

submitted it and got it right, except i forgot that each function had to operate in O(1) time which getMin does not, as i used the built in function min except that has runtime of O(n).

i cannot use a for loop to fix this because it would give runtime of O(n), and we need O(1). 
i can't sort the stack either because that would use runtime of O(n logn) and also would modify the stack which we don't want

hint 2: prefix approach to finding the minimum
hint 3: use an additional stack to maintain the prefix minimum element. when pop elements from the main stack, also pop from extra stack. but when pushing onto extra stack, should push the minimum of the top element of the extra stack and the current element onto this extra stack.

encountered a new problem, when i try to access the last element in the min_stack i don't account for the empty initial stack.

fixed that with a few if statements, but now when i pop from the stack its not technically the right value to pop

i tried to fix it by using remove instead but that has worst time complexity of O(n) which is not good.

ended up fixing it by using more if statements and checking the last element to see if it is the same, then popping that but if not then popping the top two elements but saving the last one, then appending it back onto the minstack.
```python
    class MinStack(object):

    def __init__(self):
        self.stack = []
        self.min_stack = []

    def push(self, val):
        """
        :type val: int
        :rtype: None
        """
        self.stack.append(val)
        if not self.min_stack:
            self.min_stack.append(val)
        elif val < self.min_stack[-1]:
            self.min_stack.append(val)
        else:
            min_val = self.min_stack.pop()
            self.min_stack.append(val)
            self.min_stack.append(min_val)
  

    def pop(self):
        """
        :rtype: None
        """
        if self.min_stack[-1] == self.stack[-1]:
            self.min_stack.pop()
            self.stack.pop()
        else:
            min_val = self.min_stack.pop()
            self.min_stack.pop()
            self.min_stack.append(min_val)
            self.stack.pop()
 


    def top(self):
        """
        :rtype: int
        """
        return self.stack[-1]

    def getMin(self):
        """
        :rtype: int
        """
        return self.min_stack[-1]
        
        
        
# Your MinStack object will be instantiated and called as such:
# obj = MinStack()
# obj.push(val)
# obj.pop()
# param_3 = obj.top()
# param_4 = obj.getMin()
```