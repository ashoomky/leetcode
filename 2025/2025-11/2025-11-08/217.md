my initial thought is to use a for loop to go through the elements in the inputted list, store each element in a dictionary where the key is the number, and the value is how many times it appears in the list. then check the dictionary for if there's a value greater than or equal to 2, then return true. This would give O(2n) complexity time 

i submitted my code and passed all test cases but my time complexity was reaaaally slow:
```python
    class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """

        dic = {}

        for i in range(len(nums)):
            if nums[i] in dic:
                dic[nums[i]] += 1
            else:
                dic[nums[i]] = 1
        
        for k, v in dic.items():
            if v >= 2:
                return True

        return False
```

upon further reflection i think i can make this faster by only using one for loop and looking up the amount in the dictionary.

one thing i can't give up is the for loop to iterate through the input list. i can store the values in a new list and set a variable true when current element is in the new list, and false when not found.

okay time limit exceeded with an enormous input list so it didn't pass. hm. that means my time complexity is even worse lol - but appending to a list has O(1) complexity i'm pretty sure? 

solution that did not pass because of time limit exceeding:
```python
    class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """

        result = True
        temp_list = []

        for n in nums:
            if n in temp_list:
                return True
            else:
                temp_list.append(n)
                result = False
        return result
```

since my other solution worked with a for loop through the input list that shouldn't be the problem.
my problem is probably storing each value into another list. 
so maybe a dictionary/hash map is the way to go?

okay this solution is better than the last one but still not that fast - beats 26.63%....
```python
    class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        
        dic = {}

        for i in range(len(nums)):
            if nums[i] in dic:
                return True
            else:
                dic[nums[i]] = 1

        return False
```

okay i admit i got help from aleck and i was SO CLOSE i learnt that using range(len) and indexing takes more time complexity. it makes sense that we don't actually need to access the value because we only care about how many times it appears, we don't need to return it or anything.

final solution:
```python
    class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """

        dic = {}

        for n in nums:
            if n in dic:
                return True
            else:
                dic[n] = 1

        return False
```


