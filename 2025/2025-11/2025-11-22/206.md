problem: reverse a linked list

okay so in a linked list we have the node we're currently on, previous and next. this is how the list is kept track of. once there is no next, then we have reached the end of the linked list. head is the first node of the linked list. 

maybe while iterating through the list you make the arrow point to prev?

no or maybe you go all the way to the end of the list first so you're at the last node, then go backwards and reverse it?

hm i TLE cos i don't think i have correct logic lol

okay folded solution: 

```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def reverseList(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
      
        previous, current = None, head
        while current:
            # saving the next node to set current to
            next = current.next
            #setting the pointer to the previous node to reverse
            current.next = previous
            # incrementing our previous node to the next node
            previous = current
            # incrementing the current node to the next node
            current = next
        return previous
```