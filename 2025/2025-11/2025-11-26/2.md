problem: add two numbers

given two linked lists, add the respective nodes together and return the sum of each node as a linked list.

initial thought is to store the values in a hashmap, with the index (counter) as the key. then while traversing through one linked list, assign the value to that index. then when traversing through the second linked list, simply add on the number to that key value pair and then create a new linked list from that hashmap.

i say simply... i hope it is simple...

ohmygod i misunderstood the question AGAIN
i thought it wanted me to add each individual number but we want to store the result of the sum in reverse order in a linked list ohmyyyy

my new solution is to reverse each linked list and store the value in two seperate strings, add them by converting to integers, then convert back to string and store in linked list then reverse that linked list then return it. 

YAYYY I DID IT but it only beats 5% time complexity so lemme see how i can try to optimise it haha.
```python
# Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def addTwoNumbers(self, l1, l2):
        """
        :type l1: Optional[ListNode]
        :type l2: Optional[ListNode]
        :rtype: Optional[ListNode]
        """

        # reverse linked list 1
        previous1, current1 = None, l1
        while current1:
            temp = current1.next
            current1.next = previous1
            previous1 = current1
            current1 = temp
        
        l1_integer = ""
        while previous1:
            l1_integer += str(previous1.val)
            current1 = previous1
            previous1 = previous1.next

        # reverse linked list 2
        previous2, current2 = None, l2
        while current2:
            temp = current2.next
            current2.next = previous2
            previous2 = current2
            current2 = temp
        
        l2_integer = ""
        while previous2:
            l2_integer += str(previous2.val)
            current2 = previous2
            previous2 = previous2.next

        # add the values and convert back to string
        result = int(l1_integer) + int(l2_integer)
        result = str(result)

        # iterate through string and store in linked list
        dummy = node = ListNode()
   
        for digit in result[::-1]:
            node.next = ListNode(int(digit))
            node = node.next
        return dummy.next
       
        
```

maybe i could do some stuff in one pass? well reversing the lists has to be one pass each. 
and i had to go through the reversed linked list to get the actual value. then add them, then create list hm. maybe with my method it can't be optimised anymore. 

oh wow the solution only has one while loop

they also made a dummy node, but instead of reversing stuff they just added digit by digit and had a carry! they accounted for the carry by dividing the value by 10, and obtained the last digit by modding the value by 10. they set the value to add to 0 if the list was None, otherwise it was set to the value. 

